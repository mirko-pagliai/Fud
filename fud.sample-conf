# Connection method (ssh, ftp or nfs)
METHOD="ftp"
# Server for connection
SERVER="ftp.yourhost.com"
# Username for connection (only for ssh and ftp method)
LOGIN="username"
# Password for connection (only for ssh and ftp method)
PASSWORD="password"
# Port for connection (only for ssh and ftp method)
PORT="21"
# Local directory
LPATH="/home/username/Backup"
# Remote directory
RPATH="/backup"
# Additional options for synchronizing unison
# See `man unison` for more information
BOPTIONS=""
